#!/usr/bin/env python3
"""mvault entry point — run from anywhere; adds .scripts to sys.path."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from memoryvault.cli import main

main()
